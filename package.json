{
  "name": "note-quest",
  "version": "0.1.0",
  "description": "Note Quest — kids' music note-reading game (freemium iOS PWA)",
  "scripts": {
    "build": "node scripts/prepare-www.mjs",
    "sync": "npm run build && npx cap sync ios",
    "assets": "npx @capacitor/assets generate --ios --assetPath assets",
    "test": "node --test test/*.test.cjs"
  },
  "author": "Pablo Honey",
  "license": "ISC",
  "dependencies": {
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "@capacitor/ios": "^8.4.0",
    "@revenuecat/purchases-capacitor": "^11.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
